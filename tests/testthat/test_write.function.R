# Author: Adam Rich
# Date:   2020-10-12
# Description:
#
#   Test for function
#

# Clear environment before running tests
rm(list = ls(all = TRUE))
require(alrtools)
require(testthat)


context("write.function")

test_that('write.function works', {
  # Not yet!
  expect_equal(1, 0)
})

