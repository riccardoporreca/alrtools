# Author: Adam Rich
# Date:   2020-10-12
# Description:
#
#   Test for function
#

# Clear environment before running tests
rm(list = ls(all = TRUE))
require(alrtools)
require(testthat)


context("test_template")

test_that('test_template works', {
  # Not yet!
  expect_equal(1, 0)
})

