# Author: Adam Rich
# Date:   2020-10-12
# Description:
#
#   Test for function
#

# Clear environment before running tests
rm(list = ls(all = TRUE))
require(alrtools)
require(testthat)


context("matrix2latex")

test_that('matrix2latex works', {
  # Not yet!
  expect_equal(1, 0)
})

